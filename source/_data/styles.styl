@import '../css/_schemes/Pisces/_layout';
@import '../css/_schemes/Pisces/_header';
@import '../css/_schemes/Pisces/_menu';
@import '../css/_schemes/Pisces/_sub-menu';
@import '../css/_schemes/Pisces/_sidebar';

figure.highlight {
    border-radius: $border-radius;
}

.table-container {
    border-radius: $border-radius;
}

if (hexo-config('creative_commons.post')) {
  .post-copyright ul {
      border-radius: $border-radius;
      border: 0px;
  }
}

.menu .menu-item {
    a {
        padding: 5px 30px;
    }
}

.links-of-blogroll-list {
    a {
        border-bottom: none
    }
}

.posts-expand .post-header {
    margin-bottom: 20px;
    text-align: left;
}

.post-meta {
    // padding-top: 10px;
    justify-content: left;
}

.post-block, .comments {
    padding-bottom: $content-desktop-padding - 20px;
}

.footer {
    padding: 10px 0;
}

.posts-expand .post-meta-container {
    time {
        border-bottom: 0px;
    }
}

.powered-by {
    a {
        border-bottom: 0px;
    }
}

/*
.btn {
    padding: 0;
    border: 0;
    &:hover {
        background: var(--btn-default-bg);
        color: var(--btn-default-color);
    }
}

.post-button {
    margin-top: 0px;
    text-align: left;
}*/

.pagination {
    .prev, .next, .page-number {
        padding: 0;
        display: inline-block;
        margin: 0 .5em;
        width: 2.5em;
        height: 2.5em;
        line-height: 2.5;
        border: 0;
        border-radius: 100%;
        // background: var(--content-bg-color);
        transition-property: background;
        transition-duration: .2s;
        transition-timing-function: ease-in-out;
        transition-delay: 0s;

        &:hover {
            color: $pagination-active-color;
            background: $pagination-active-bg;
        }
    }
}

.back-to-top {
    // border-radius: 100%;
}

.main-inner > {
    .pagination {
        background-color: transparent;
    }
}

+desktop() {
    .main-inner > {
        .pagination {
            padding: 0;
        }
    }

    .headband {
        display: none;
    }

    .main-inner {
        margin-top: $sidebar-offset;
    }

    .header {
        margin-top: $sidebar-offset;
    }

    .site-brand-container {
        display: none;
    }
}

if (hexo-config('toc.enable')) {
  .post-toc {
        a {
            border-bottom: none;
        }
    }
}

:root {
    /*
    --sys-font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Droid Sans", "Helvetica Neue";
    --zh-font-family: "PingFang SC", "Hiragino Sans GB", "Droid Sans Fallback", "Microsoft YaHei";
    --base-font-family: "Lato", var(--sys-font-family), var(--zh-font-family), sans-serif;
    --code-font-family: Menlo, Monaco, Consolas, "Courier New", monospace;
    font-family: var(--base-font-family);
    */
    -webkit-font-smoothing: antialiased;
    --animate-delay: 0.5s;
    --animate-duration: 0.5s;
}